# Tasks Tracking Todo

- [x] Analyze and trace all shadow files (`-1` versions and suffix duplicates) inside `/android` and `/ios` directories
- [x] Move, organize, and cleanly replace active android configurations with custom user assets
- [x] Strip Windows-specific properties (`org.gradle.java.home`) from `gradle.properties` files to ensure build toolchains remain cross-platform compatible
- [x] Investigate missing logo issues on Android and verify asset catalog status on iOS
- [x] Map custom launcher mipmaps over standard targets to replace default/blank placeholders
- [x] Investigate and fix 'Failed to fetch' error when joining families inside native Android devices
- [x] Implement robust dynamic domain resolution in AuthModal by letting users input/paste their invitation links directly, solving both endpoint configuration and link ingestion
- [x] Clean up temporary explorer search scripts and perform final linter / compiler verification passes
